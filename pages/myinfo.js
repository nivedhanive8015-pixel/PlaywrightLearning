import { expect } from "@playwright/test";
export class MyInfo{
    constructor(page){
        this.page =page;
        this.myInfoLink = page.getByRole('link',{name :"My Info",exact:true});
        this.myInfoHeading=page.getByRole('heading',{name :"PIM",exact :true});
        this.employeeHeading =page.getByRole('heading',{name:"manda user",exact :true});
        this.attachmentHeading =page.getByRole('heading',{name :"Attachments",exact :true});
        this.addAttachment =page.getByRole('button',{name :" Add "});
        this.addAttachmentHeading =page.getByRole('heading',{name :"Add Attachment",exact:true});
        this.selectFile= page.getByText("Select File")
        .locator('xpath=ancestor::div[contains(@class,"oxd-input-group")]');
        this.fileInput = this.selectFile.locator('input[type="file"]');
        this.browserContainer =this.selectFile.locator('.oxd-file-div');
        this.browser =this.browserContainer.locator('.oxd-file-button');
        this.NoFiles =this.browserContainer.locator('.oxd-file-input-div');
        this.addFiles=this.selectFile.locator('.oxd-icon.bi-upload.oxd-file-input-icon');
        this.comment=page.getByText("Comment")
        .locator('xpath=ancestor::div[contains(@class,"oxd-input-group")]');
        this.typeComment=this.comment.getByPlaceholder("Type comment here");
        this.addAttachmentCard = this.addAttachmentHeading.locator('xpath=ancestor::div[contains(@class,"orangehrm-card-container")]');
        this.addAttachmentForm = this.fileInput.locator('xpath=ancestor::form');   
        this.save = this.addAttachmentForm.getByRole('button', {name: "Save",exact: true});
     
        

    }

        async openMyInfo(){
        await  this.myInfoLink.click();
        await expect(this.myInfoHeading).toBeVisible();
        await expect(this.employeeHeading).toBeVisible();
        await expect(this.attachmentHeading).toBeVisible();
        }

        async openAddAttachment(){
        await this.addAttachment.click();
        await expect(this.addAttachmentHeading).toBeVisible();
        }

        async uploadFiles(filePath){
            await this.fileInput.setInputFiles(filePath);     
     }

        async addComments(entercomment){
            await this.typeComment.fill(entercomment);
        }

        async saveInfo(){
             console.log("Save count:", await this.save.count());
            await this.save.click();
        }

        }
      








































































































































































































































































































































































































































































































        




























































































































































































































































    











































































































































































    
